export const loginUser = (req,res)=>{
    res.send("loginUser Controller is working ")
}

export const signinUser = (req , res ) => {
    res.send("Sign up controller is working ")
}

export const LogoutUser = (req , res ) => {
    res.send("Logout controler is working")
}